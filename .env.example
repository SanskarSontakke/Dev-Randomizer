# No environment variables are required to run or deploy this app.
#
# Optional: override the canonical site URL used for metadata (Open Graph,
# Twitter cards). If unset, it's derived automatically from Vercel's
# VERCEL_PROJECT_PRODUCTION_URL / VERCEL_URL, or falls back to localhost.
# NEXT_PUBLIC_SITE_URL=https://your-domain.example.com
